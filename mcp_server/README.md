# mcp_server

MCP-сервер на Go: поиск по Qdrant для LLM.

В Sprint 1 это заготовка. Tools (`search_records`, `get_contract`,
`list_obligations`, `budget_summary`, `ingest_status`) появятся в Sprint 3.

```bash
go run .
```

Сейчас завершается с кодом 2.
