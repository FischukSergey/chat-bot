# Протокол репетиции Sprint 5 (с нуля)

Дата: 2026-09-19. Машина: macOS, Docker Desktop.
Сценарий — по [README.md](../README.md), не по памяти.
Контрольный файл: `data/incoming/БДР для индексации.xlsx`.
Реестры не индексировали (scope v1 — только бюджет).

## Шаги

| шаг | команда | время | результат |
|---|---|---|---|
| 1 | `task local:down:clean` | ~8 с | volume `qdrant-data` удалён |
| 2 | `task local:up` | ~14 с | qdrant → mcp → assistant `(healthy)` |
| 3 | `GET :7345/health` и `:7346/health` | — | ok, `points: 0` (коллекции ещё нет) |
| 4 | `task local:ingest` | 8.6 с | принято 172, обновлено 0, rejects 0 |
| 5 | три вопроса `POST /chat` | см. ниже | цифры сошлись с золотым набором |
| 6 | `task local:ingest` повтор | ~11 с | принято 0, обновлено 172, точек **172** |

LM Studio на хосте `:1234` был включён
(`qwen/qwen3.6-35b-a3b`, `text-embedding-qwen3-embedding-0.6b`).

## Вопросы (эталоны из `data/eval/gold.yaml`)

### `code-1.8.2` (~22 с)

Лимит **124970.30** тыс. руб., `production:1.8.2`, row 23,
факт/принятые/`remain_free` не заполнены. `tools_used`: `search_records`.
Модель также написала сумму ×1000 в рублях — известный лимит v1, эталон не ломает.

### `refuse-9.9.9` (~4 с)

«В индексе нет записей…». `sources: []`, без сумм. `search_records`.

### `code-5.1.1.1` (~6 с)

Лимит **681351.76** / 681351.76452 тыс. руб., row 66,
`production:5.1.1.1`. Пустые факт/принятые/остаток не подменены лимитом.

## Грабли

- `task local:down:clean` стирает индекс. На демо предупреждать оператора.
- Без LM Studio `up --wait` дождётся healthy только если `/health` видит embeddings;
  на этой машине сервер был уже запущен.
- Хостовые `task mcp` / `task assistant -- serve` не поднимали — порты занял compose.
- `Бизнес-план …xlsx` не грузили.

Полный прогон золотого набора (25 вопросов) в этой репетиции не делали —
три кейса из чек-листа пункта 5.
