# assistant

Python-оркестратор RAG: вопрос пользователя, вызов MCP, ответ со источниками.

В Sprint 1 это заготовка. CLI-чат и `POST /chat` появятся в Sprint 4.
LLM — LM Studio (`localhost:1234/v1`).

```bash
python3 -m assistant
```

Сейчас завершается с кодом 2 и сообщением, что реализация ещё не готова.
